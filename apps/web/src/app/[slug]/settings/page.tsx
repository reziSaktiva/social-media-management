import { ScaffoldPlaceholder } from "@/components/ScaffoldPlaceholder";

export default function Page() {
  return (
    <ScaffoldPlaceholder
      title="Settings — General"
      message="Placeholder — implementasi direncanakan di T-008 (Workspace Settings — General + Danger Zone)."
    />
  );
}
